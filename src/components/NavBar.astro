---
import Menu from "../assets/Menu.svg";
import MenuButton from "./MenuButton";
---

<nav
    class="relative flex justify-between items-center h-full border-2 border-primary rounded-sm px-2 backdrop-blur-sm bg-secondary/50 lg:grow"
>
    <div class="hidden lg:block w-full">
        <ul class="flex justify-between gap-2 font-headings">
            <a href="#Home">Home</a>
            <a href="#Experience">Experiencia</a>
            <a href="#Technologie">Tech Stack</a>
            <a href="#Projects">Proyectos</a>
            <a href="/public/Carlos_Mendoza_C_V_.pdf" class="button-primary hover:scale-105 active:brightness-125 transition-all duration-150">Mi C.V. </a>
        </ul>
    </div>
    <div class="block lg:hidden h-full w-auto">
        <MenuButton client:visible >
            <Menu/>
        </MenuButton>
    </div>
</nav>

<script>
    import { gsap } from "gsap";
    import { ScrollToPlugin } from "gsap/ScrollToPlugin";

    gsap.registerPlugin(ScrollToPlugin);

    const links = document.querySelectorAll<HTMLAnchorElement>("nav a");

    function scrollSection(ref: string) {
        gsap.to(window, { duration: 1, scrollTo: ref, ease: "power2.inOut" });
    }

    links.forEach((element) => {
        const href = element.getAttribute("href") ?? "";

        if (!href.startsWith("#")) return;

        element.addEventListener("click", (e) => {
            e.preventDefault();
            scrollSection(href);
        });
    });
</script>
